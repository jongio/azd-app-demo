{
  "name": "demo-api",
  "version": "1.0.0",
  "description": "Demo API with intentional bug for AI debugging and testing demonstration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "c8 --reporter=text --reporter=html node --test",
    "test:unit": "c8 --reporter=text --reporter=html node --test server.test.js",
    "test:integration": "node -e \"console.log('No integration tests configured')\""
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
